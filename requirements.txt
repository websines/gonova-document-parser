# Core dependencies for document processing
# Install with: uv pip install -r requirements.txt

# Docling (main framework)
docling[vlm]>=2.0.0

# Deep learning
transformers>=4.46.0
torch>=2.6.0
flash-attn>=2.7.0  # For faster inference

# PDF processing
pypdf>=5.0.0
pdf2image>=1.17.0
pillow>=10.0.0

# Configuration & utilities
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# CLI & logging
rich>=13.0.0
typer>=0.12.0
loguru>=0.7.0

# API server (required for REST API endpoints)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Embeddings (Qwen3-Embedding-0.6B)
sentence-transformers>=3.3.0

# Optional: vLLM for production (uncomment if using vLLM hosting)
# vllm>=0.6.0
# openai>=1.0.0

# Optional: Database integration (uncomment if using graph-vector DB)
# neo4j>=5.0.0
# redis>=5.0.0
