# Core dependencies for document processing with MinerU 2.5
# Install with: pip install -r requirements.txt

# MinerU client library (no model hosting, just client)
mineru-vl-utils>=0.1.0

# PDF processing (PyMuPDF is 3-5x faster than pdf2image)
pymupdf>=1.24.0
pillow>=10.0.0

# Configuration & utilities
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# CLI & logging
rich>=13.0.0
typer>=0.12.0
loguru>=0.7.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Async HTTP client for vLLM server communication
aiofiles>=24.0.0
httpx>=0.27.0

# Redis for job queue
redis>=5.0.0
rq>=1.15.0

# Optional: Embeddings (Qwen3-Embedding-0.6B)
# sentence-transformers>=3.3.0
